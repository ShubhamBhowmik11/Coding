#include<bits/stdc++.h>
using namespace std;
int main(){
// positive to negative
      int test = 5;
      int n = ~test;
      n = ~n;
 //msb(most significant bit) turn 1,tha t represent neagative number
   cout<<n<<'\n';
// ~0 is -1 because of above reason
 cout<<~0;
 
    
}